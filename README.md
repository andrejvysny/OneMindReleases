# OneMindReleases

Public release binaries and updater manifest for [OneMind](https://github.com/andrejvysny/OneMind).

- `latest.json` — Tauri updater manifest (auto-updated on each release)
- Releases contain macOS ARM binaries (`.app.tar.gz` + `.sig`)
